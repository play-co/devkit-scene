import .Actor;
import .SATActor;

/** @lends scene */
exports = {

  /**
   * Easy access to actor classes
   * @property {Actor}    Actor
   * @property {SATActor} SAT
   */
  actor: {
    Actor: Actor,
    SAT: SATActor
  }

};
